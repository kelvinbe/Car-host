
const FETCH_DATA = "SELECT * FROM auth_code WHERE status = ?";

const SEARCH_LOCALLY = "";

const SEARCH_BY_HOST = "";

const FETCH_HISTORY = "";

const VERIFTY_AUTH_CODE = "";

export {
    FETCH_DATA,
    SEARCH_LOCALLY,
    SEARCH_BY_HOST,
    FETCH_HISTORY,
    VERIFTY_AUTH_CODE
};